{
  "name": "sine-wave-tts",
  "version": "0.1.0",
  "description": "Makes text speak in beeping sine-wave tones. A voice for robots, AI characters, and mascots that aren't human.",
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    },
    "./server": {
      "types": "./dist/src/server/index.d.ts",
      "import": "./dist/src/server/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "webdemo": "node scripts/sync-kuromoji-dict.mjs && vite demo",
    "webdemo:build": "node scripts/sync-kuromoji-dict.mjs && vite build demo",
    "serve": "npm run build --silent && node dist/src/server/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "kuromoji": "^0.1.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "@types/kuromoji": "^0.1.3",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
